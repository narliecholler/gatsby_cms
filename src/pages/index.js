import React from 'react'
import { Heading, Box } from '@chakra-ui/core';

export default () => (
  <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh', width: '100vw', backgroundColor: '#f8f2e7', color: '#861f41' }}>
    <Box>
      <Heading as="h1" size="xl">Site is currently in development, hang tight!</Heading>
    </Box>
  </div >
)